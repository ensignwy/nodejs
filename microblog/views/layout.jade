//注释：layout是模板引擎提供的一个页面布局模板，默认所以页面都继承自这个模板，替换掉 block content部分（固定的写法）
doctype html
html
  head
    title= title
    link(rel='stylesheet', href='/stylesheets/bootstrap.css')
    style.
      body{
        padding-top:60px;
        padding-bottom:40px;
      }
  body
    div.navbar.navbar-fixed-top.navbar-inverse
        div(class="container")
          div.navbar-header
            button(class="navbar-toggle collapsed" data-toggle="collapse" data-target=".nav-collapse")
              span(class="icon-bar")
              span(class="icon-bar")
              span(class="icon-bar")
            a(class="navbar-brand" href="/") Microblog
          div(class="navbar-collapse collapse")
            ul(class="nav navbar-nav")
              li(class="active")
                a(href="/")首页
              if !user
                li
                  a(href="/login")登录
                li
                  a(href="/reg")注册
              else
                li
                  a(href="/logout")登出
                li
                  a(href='/u/'+user.name)我的
            if (user) 
              ul(class="nav navbar-nav navbar-right")
                  li 
                    a(href='#') 你好, #{user.name}
                    
    div(class="container" id="container")
      block content
      hr
      fotter
        p 
          a(href="#") CopyRight 2018
